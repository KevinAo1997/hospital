<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aokai.hospital.dao.PatientMapper">
  <resultMap id="BaseResultMap" type="com.aokai.hospital.po.Patient">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="email" jdbcType="VARCHAR" property="email" />
    <result column="name" jdbcType="VARCHAR" property="name" />
  </resultMap>
  <select id="checkPatient" resultType="com.aokai.hospital.po.Patient">
    select * from patient where username = #{username} and password = #{password}
  </select>
  <select id="checkUserName" resultType="com.aokai.hospital.po.Patient">
    select * from patient where username = #{username}
  </select>
  <select id="checkUserEmail" resultType="com.aokai.hospital.po.Patient">
    select * from patient where email = #{email}
  </select>
</mapper>
